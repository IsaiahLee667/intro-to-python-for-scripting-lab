#Ask for user name, then store response as a variable
name = input("Hello, what is your name? ")

#Reply with variable, using a Fstring
print(f"Nice to meet you, {name}")

#Remind myself/Finally type aloud meaning of the git stuff
# git add . = Git add all in current rep
# git commit -m commits added stuff to git, -m notes a message is added providing context. 
# if not adding -m remove the string in quotes and it will still work
# git push origin main - Pushes all added content to the origin main.